.TH _PRINTF 3 "21/07/2025 -> 25/07/2025" "version 1.0"
.SH NAME
_printf \- custom implementation of printf to print formatted output to standard output

.SH SYNOPSIS
.B #include "main.h"
.br
.B int _printf(const char *format, ...);

.SH DESCRIPTION
The
.B _printf()
function produces output according to a format string and additional arguments, similar to the standard
.B printf(3)
function.

It supports the following format specifiers:

.TP
.B %c
Prints a single character.

.TP
.B %s
Prints a null-terminated string of characters.

.TP
.B %%
Prints a literal percent sign.

.TP
.B %d
Prints a signed decimal integer.

.TP
.B %i
Same as %d; prints a signed decimal integer.

.TP
.B %o
Know to print other % but print just %o.

.SH RETURN VALUE
Returns the total number of characters printed.
Returns \-1 on error.

.SH EXAMPLES
.nf
_printf("Char: %c\\n", 'A');
_printf("String: %s\\n", "Hello");
_printf("Integer: %d\\n", 42);
_printf("Percent: %%\\n");
_printf("other format: %o\\n);
.fi

.SH AUTHOR
Written by [Sorli Thomas an Cecile Laroche]
